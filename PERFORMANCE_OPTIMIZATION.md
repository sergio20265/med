# Оптимизация производительности Angular приложения

## Выполненные оптимизации

### 1. Настройки сборки Angular

#### Production конфигурация
- ✅ **Build Optimizer**: Включен для дополнительной оптимизации кода
- ✅ **AOT компиляция**: Включена для предварительной компиляции шаблонов
- ✅ **Tree Shaking**: Автоматическое удаление неиспользуемого кода
- ✅ **Минификация**: Включена для JavaScript, CSS и шрифтов
- ✅ **Critical CSS**: Встраивание критических стилей для быстрой загрузки
- ✅ **Vendor Chunk**: Отключен для уменьшения количества файлов
- ✅ **Source Maps**: Отключены для production

#### Development конфигурация
- ✅ **Частичная оптимизация**: Включена минификация стилей и скриптов
- ✅ **Source Maps**: Отключены для ускорения сборки
- ✅ **AOT**: Включен для консистентности с production

### 2. Бюджеты размера файлов
- **Initial bundle**: Предупреждение при 2MB, ошибка при 5MB
- **Component styles**: Предупреждение при 6KB, ошибка при 10KB

## Дополнительные рекомендации

### 3. Оптимизация компонентов

```typescript
// Используйте OnPush стратегию для компонентов
@Component({
  changeDetection: ChangeDetectionStrategy.OnPush
})

// Используйте trackBy для *ngFor
trackByFn(index: number, item: any) {
  return item.id;
}
```

### 4. Lazy Loading модулей

```typescript
// В app.routes.ts
const routes: Routes = [
  {
    path: 'feature',
    loadChildren: () => import('./feature/feature.module').then(m => m.FeatureModule)
  }
];
```

### 5. Оптимизация изображений
- Используйте WebP формат для современных браузеров
- Добавьте lazy loading для изображений
- Оптимизируйте размеры изображений

### 6. Service Worker
```bash
# Добавить Service Worker для кэширования
ng add @angular/pwa
```

### 7. Preloading стратегии
```typescript
// В app.config.ts
import { PreloadAllModules } from '@angular/router';

provideRouter(routes, withPreloading(PreloadAllModules))
```

### 8. Bundle анализ
```bash
# Анализ размера bundle
ng build --stats-json
npx webpack-bundle-analyzer dist/stacionar-site/stats.json
```

### 9. Оптимизация шрифтов
- ✅ Используются Google Fonts с preconnect
- ✅ Добавлен display=swap для быстрой загрузки
- ✅ Загружаются только необходимые веса шрифтов

### 10. Мониторинг производительности

```typescript
// Добавить Web Vitals мониторинг
import { getCLS, getFID, getFCP, getLCP, getTTFB } from 'web-vitals';

getCLS(console.log);
getFID(console.log);
getFCP(console.log);
getLCP(console.log);
getTTFB(console.log);
```

## Результаты оптимизации

### Ожидаемые улучшения:
- 🚀 **Время загрузки**: Уменьшение на 30-50%
- 📦 **Размер bundle**: Уменьшение на 20-40%
- ⚡ **Time to Interactive**: Улучшение на 25-35%
- 🎯 **Lighthouse Score**: Повышение до 90+

### Команды для сборки

```bash
# Development с оптимизацией
ng serve

# Production сборка
ng build --configuration production

# SSR Production сборка
npm run build:ssr

# Полная сборка с sitemap
npm run build:prod
```

## Мониторинг

- Используйте Chrome DevTools для анализа производительности
- Регулярно проверяйте Lighthouse Score
- Мониторьте Core Web Vitals
- Анализируйте Network tab для оптимизации загрузки ресурсов