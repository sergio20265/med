# Настройка уведомлений в Telegram

Для получения уведомлений о новых заявках в Telegram необходимо выполнить следующие шаги:

## 1. Создание Telegram бота

1. Найдите в Telegram бота @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания нового бота
4. Сохраните полученный токен бота

## 2. Получение Chat ID

⚠️ **Важно**: Chat ID должен быть вашего личного чата или группы, НЕ другого бота!

### Способ 1: Через @userinfobot
1. Найдите в Telegram бота @userinfobot
2. Отправьте ему любое сообщение
3. Бот вернет ваш личный Chat ID

### Способ 2: Через API (рекомендуемый)
1. Отправьте сообщение `/start` вашему созданному боту
2. Перейдите по ссылке: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. Найдите в ответе поле `"chat":{"id":ВАШЕ_CHAT_ID}`
4. Используйте этот Chat ID (это будет ваш личный чат с ботом)

### Способ 3: Для группы
1. Добавьте бота в группу
2. Отправьте любое сообщение в группу
3. Используйте API метод выше для получения Chat ID группы

## 3. Настройка environment файла

Откройте файл `src/app/environments/environment.ts` и замените:

```typescript
telegramBotToken: 'YOUR_BOT_TOKEN_HERE',
telegramChatIds: ['YOUR_CHAT_ID_HERE'],
```

На ваши реальные значения:

### Для одного чата:
```typescript
telegramBotToken: '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz',
telegramChatIds: ['123456789'],
```

### Для нескольких чатов:
```typescript
telegramBotToken: '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz',
telegramChatIds: ['123456789', '987654321', '-1001234567890'], // личные чаты и группы
```

⚠️ **Примечание**: Теперь поддерживается отправка уведомлений в несколько чатов одновременно. Просто добавьте все нужные Chat ID в массив `telegramChatIds`.

## 4. Формат уведомлений

После настройки вы будете получать уведомления в следующем формате:

```
🏥 Новая заявка на консультацию

👤 Имя: Иван Иванов
📞 Телефон: +7 (999) 123-45-67

📅 Время подачи: 15.12.2024, 14:30:25
```

## 5. Безопасность

⚠️ **Важно**: Никогда не публикуйте токен бота в публичных репозиториях!

Для продакшена рекомендуется:
- Использовать переменные окружения
- Создать отдельный `environment.prod.ts` файл
- Добавить токены в `.env` файл и исключить его из git

## Проверка работы

1. Запустите приложение
2. Заполните и отправьте форму записи на консультацию
3. Проверьте, что уведомление пришло в Telegram
4. В консоли браузера должно появиться сообщение "Уведомление в Telegram отправлено успешно"

## Устранение ошибок

### Ошибка 403: "Forbidden: bots can't send messages to bots"
**Причина**: Указан Chat ID другого бота вместо личного чата или группы.
**Решение**: Используйте Chat ID вашего личного чата с ботом или группы, куда добавлен бот.

### Ошибка 400: "Bad Request: chat not found"
**Причина**: Неверный Chat ID или бот не имеет доступа к чату.
**Решение**: 
1. Проверьте правильность Chat ID
2. Убедитесь, что вы отправили `/start` боту
3. Для групп - убедитесь, что бот добавлен в группу

### Ошибка 401: "Unauthorized"
**Причина**: Неверный токен бота.
**Решение**: Проверьте правильность токена в файле environment.ts

### Уведомления не приходят
**Возможные причины**:
1. Не заполнены `telegramBotToken` или `telegramChatIds` в environment.ts
2. Значения указаны как 'YOUR_BOT_TOKEN_HERE' или массив содержит 'YOUR_CHAT_ID_HERE'
3. Массив `telegramChatIds` пустой или содержит некорректные Chat ID
4. Проблемы с сетью или API Telegram

**Решение**: 
- Проверьте консоль браузера на наличие ошибок при отправке формы
- Убедитесь, что массив `telegramChatIds` содержит корректные Chat ID
- Проверьте, что все Chat ID в массиве действительны и бот имеет доступ к этим чатам