# 🐳 Запуск приложения в Docker

Это руководство поможет вам запустить Angular SSR приложение в Docker контейнере.

## 📋 Предварительные требования

- Docker (версия 20.10 или выше)
- Docker Compose (версия 2.0 или выше)

## 🚀 Быстрый старт

### 1. Сборка и запуск с Docker Compose (рекомендуется)

```bash
# Клонируйте репозиторий (если еще не сделали)
git clone <repository-url>
cd stacionar-site

# Запустите приложение
docker-compose up --build
```

Приложение будет доступно по адресу: http://localhost:4000

### 2. Запуск только приложения (без Nginx)

```bash
# Сборка образа
docker build -t stacionar-site .

# Запуск контейнера
docker run -p 4000:4000 --name stacionar-app stacionar-site
```

### 3. Production запуск с Nginx

```bash
# Запуск с Nginx (для production)
docker-compose --profile production up --build
```

Приложение будет доступно по адресу: http://localhost:80

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# API токен для генерации sitemap
API_TOKEN=your-api-token-here

# Режим работы Node.js
NODE_ENV=production
```

### Настройка портов

По умолчанию используются следующие порты:
- **4000** - Angular SSR приложение
- **80** - Nginx (только в production режиме)
- **443** - HTTPS Nginx (требует SSL сертификаты)

Для изменения портов отредактируйте `docker-compose.yml`:

```yaml
services:
  stacionar-app:
    ports:
      - "8080:4000"  # Изменить на нужный порт
```

## 🏗️ Архитектура Docker

### Многоэтапная сборка

Docker использует многоэтапную сборку для оптимизации:

1. **Builder stage** - установка зависимостей и сборка приложения
2. **Production stage** - минимальный образ только с необходимыми файлами

### Безопасность

- Использование непривилегированного пользователя `angular`
- Минимальный Alpine Linux образ
- Только production зависимости в финальном образе

## 📊 Мониторинг и логи

### Просмотр логов

```bash
# Логи приложения
docker-compose logs -f stacionar-app

# Логи Nginx
docker-compose logs -f nginx

# Все логи
docker-compose logs -f
```

### Health Check

Контейнер включает проверку здоровья:

```bash
# Проверка статуса
docker ps

# Детальная информация о health check
docker inspect stacionar-app | grep -A 10 Health
```

## 🛠️ Команды для разработки

### Остановка и удаление

```bash
# Остановка
docker-compose down

# Остановка с удалением volumes
docker-compose down -v

# Полная очистка
docker-compose down --rmi all --volumes --remove-orphans
```

### Пересборка

```bash
# Пересборка без кэша
docker-compose build --no-cache

# Пересборка и запуск
docker-compose up --build --force-recreate
```

### Выполнение команд в контейнере

```bash
# Подключение к контейнеру
docker-compose exec stacionar-app sh

# Выполнение команды
docker-compose exec stacionar-app yarn generate:sitemap
```

## 🔧 Troubleshooting

### Проблема с портами

```bash
# Проверка занятых портов
lsof -i :4000

# Остановка процесса на порту
kill -9 $(lsof -t -i:4000)
```

### Проблемы с памятью

```bash
# Увеличение лимита памяти для Docker
docker run --memory=2g -p 4000:4000 stacionar-site
```

### Очистка Docker

```bash
# Удаление неиспользуемых образов
docker image prune -a

# Полная очистка Docker
docker system prune -a --volumes
```

## 📈 Production развертывание

### С SSL сертификатами

1. Поместите SSL сертификаты в папку `ssl/`:
   ```
   ssl/
   ├── cert.pem
   └── key.pem
   ```

2. Раскомментируйте HTTPS секцию в `nginx.conf`

3. Запустите с production профилем:
   ```bash
   docker-compose --profile production up -d
   ```

### Автоматический перезапуск

Контейнеры настроены на автоматический перезапуск (`restart: unless-stopped`)

### Мониторинг в production

```bash
# Статистика использования ресурсов
docker stats

# Мониторинг логов в реальном времени
docker-compose logs -f --tail=100
```

## 🤝 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что порты свободны
3. Проверьте наличие всех необходимых файлов
4. Попробуйте пересобрать образ: `docker-compose build --no-cache`

---

**Примечание**: Для production использования рекомендуется настроить SSL сертификаты и использовать внешнюю базу данных, если требуется.